"use client";
import { useForm } from "react-hook-form"; import { zodResolver } from "@hookform/resolvers/zod"; import { authSchema } from "@/lib/schemas"; import { Card } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import type { z } from "zod";
export function AuthCard({mode}:{mode:"login"|"register"|"forgot"}){const {register,handleSubmit,formState:{errors}}=useForm<z.infer<typeof authSchema>>({resolver:zodResolver(authSchema)});return <main className="grid min-h-screen place-items-center p-6"><Card className="w-full max-w-md"><h1 className="text-2xl font-bold">{mode==="login"?"Welcome back":mode==="register"?"Create your workspace":"Reset password"}</h1><p className="mt-2 text-sm text-[var(--muted)]">Supabase Auth ready form with email verification and password recovery flows.</p><form className="mt-6 space-y-4" onSubmit={handleSubmit(()=>undefined)}><input className="w-full rounded-xl border border-[var(--border)] bg-transparent px-4 py-3" placeholder="Email" {...register("email")}/>{errors.email&&<p className="text-sm text-red-300">{errors.email.message}</p>}{mode!=="forgot"&&<input className="w-full rounded-xl border border-[var(--border)] bg-transparent px-4 py-3" placeholder="Password" type="password" {...register("password")}/>}<Button className="w-full bg-violet-500 text-white">Continue</Button></form><div className="mt-4 flex justify-between text-sm text-[var(--muted)]"><a href="/auth/login">Login</a><a href="/auth/register">Register</a><a href="/auth/forgot-password">Forgot</a></div></Card></main>}
